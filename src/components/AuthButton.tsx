'use client'

// Stub — replace body with usePrivy() once NEXT_PUBLIC_PRIVY_APP_ID is set
export default function AuthButton() {
  return (
    <button className="text-[10px] font-label font-bold uppercase tracking-widest opacity-70 hover:opacity-100 hover:text-primary transition-all">
      Sign In
    </button>
  )
}
